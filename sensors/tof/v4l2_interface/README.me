# V4L2 Interface to the ToF Camera

This directory uses code from another git repository (https://github.com/analogdevicesinc/aditof_sdk)

To get this code into your working directory, use `git submodule init` and `git submodule update`

